"""Настраиваемые константы вертикальной сшивки этажей (единственное место объявления)."""

R_SNAP_PX = 12
"""Радиус привязки новой контрольной точки к ближайшей вершине стены."""

R_HIT_PX = 10
"""Клик в этом радиусе выбирает существующую точку, а не добавляет новую."""

R_MIN_BASELINE_FRAC = 0.02
"""Минимальный разброс контрольных точек как доля диагонали изображения секции."""

MIN_CONTROL_POINTS = 3
"""Секция решается только при наличии >= 3 сопоставленных точек."""

MAX_CONTROL_POINTS = 20
"""Лимит контрольных точек на секцию."""

MAX_CONNECTORS = 50
"""Лимит коннекторов на этаж."""

MAX_CONNECTOR_POINTS = 64
"""Максимум вершин в ломаной коннектора."""

MAX_CUTOUTS = 50
"""Лимит зон-вырезов на этаж."""

MAX_CUTOUT_POINTS = 64
"""Максимум вершин в полигоне выреза (прямоугольник = 4)."""

MAX_FLOOR_CANVAS_PX = 4000
"""Ограничение длинной стороны собранного холста (защита по памяти, мастер-пиксели)."""

DETAIL_WARN_SCALE = 0.5
"""Предупреждение, когда решённый масштаб секции падает ниже этого значения."""

PPM_WARN_RATIO = 0.10
"""Расхождение ppm между секциями, дающее некритичное предупреждение."""

FLOOR_HEIGHT = 3.0
"""Высота выдавливания (метры) для build_mesh_from_mask."""

INTER_FLOOR_GAP_M = 0.2
"""Зазор (метры) между этажами в 3D, чтобы плита верхнего этажа не пробивала стены нижнего. Шаг укладки = FLOOR_HEIGHT + INTER_FLOOR_GAP_M, одинаково для меша и для высоты маршрута."""

RESIDUAL_WARN_M = 0.5
"""Невязка решения считается большой выше 0.5 м (метрический, не зависит от масштаба)."""

CANVAS_TRUST_RESIDUAL_M = 3.0
"""Максимальная невязка регистрации (метры), при которой секция влияет на масштаб холста k. Плохо совмещённая секция занижает свой масштаб и раздула бы k на весь этаж, поэтому такие секции из оценки k исключаются (но всё равно строятся)."""

DEFAULT_CONNECTOR_THICKNESS_M = 0.15
"""Толщина стены коннектора по умолчанию, когда в линии не задан thickness_m."""

MIN_CONNECTOR_THICKNESS_PX = 3
"""Минимальная видимая толщина стены коннектора в пикселях. При малом ppm метрическая толщина округлилась бы до 1 px и коннектор стал бы невидимым волоском."""

MATCH_TOLERANCE_M = 1.5
"""Радиус сопоставления шахт в системе координат здания (метры): лестница/лифт этажа N парится с этажом N+-1, если их XY ближе этого расстояния."""

TRANSITION_COST_M = 3.0
"""Стоимость (метры) одного перехода по лестнице/лифту в объединённом графе. По умолчанию равна FLOOR_HEIGHT, т.е. один этаж."""
